# Lattice-surgery compiler outputs

This directory is reserved for the isolated compiler smoke test in
`tools/lattice_surgery/`.

Generated reports, summaries, and complete patch-layout slice files are ignored
by Git. The editable QASM input is tracked separately at
`tools/lattice_surgery/input_circuit.qasm`. This README is retained so the
output location exists in a fresh clone.
